#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 *
 * Return: Always returns 0
 */
int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
